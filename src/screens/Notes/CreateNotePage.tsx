import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const CreateNotePage = (props: Props) => {
  return (
    <View>
      <Text>CreateNotePage</Text>
    </View>
  )
}

export default CreateNotePage

const styles = StyleSheet.create({})