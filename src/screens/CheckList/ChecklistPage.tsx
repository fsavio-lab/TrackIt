import { StyleSheet, Text, View } from 'react-native'
import * as React from 'react'

type Props = {}

const ChecklistPage = (props: Props) => {
  return (
    <View>
      <Text>ChecklistPage</Text>
    </View>
  )
}

export default ChecklistPage

const styles = StyleSheet.create({})